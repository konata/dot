export DOT_HOME="${DOT_HOME:-$HOME/dot}"
[[ -r ~/.config/zsh/init.zsh ]] && source ~/.config/zsh/init.zsh

# Keep third-party installer edits below this line, or let installers append to
# the real ~/.zshrc directly. The tracked shell state lives under $DOT_HOME.

